# HTTP
aiohttp==3.9.5

# FastAPI
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Pydantic + settings
pydantic==2.7.1
pydantic-settings==2.3.1

# PostgreSQL — psycopg 3 driver with C speedups and a connection pool
psycopg[binary,pool]==3.2.9

# Redis — cache-aside layer for read-heavy API queries
redis==5.0.8
